doctype html
html
  head
    meta(charset="utf8")
    title #{site.data.resume.page.pageTitle}
    meta(name="description", content=site.data.resume.pageDescription)
      
    if config.entry.css.external
      link(rel="stylesheet", href="style.css")
  body
    .container
      block content
